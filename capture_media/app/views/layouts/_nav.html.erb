<ul class="navbar navbar-toggleable-sm navbar-inverse bg-inverse">
  <li>
    <%= link_to "Home", root_path, class: "nav-link" %>
  </li>
  <% if current_user %>
      <li class="nav-item dropdown justify-content-end">
        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Welcome, <%= current_user.first_name %></a>
        <div class="dropdown-menu">
          <%= link_to "My Appointments", user_appointments_path(current_user), class: 'dropdown-item', id: 'appointments' %>
          <%= link_to "My Packages", packages_path, class: 'dropdown-item' %>
          <div class="dropdown-divider"></div>
          <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
        </div>
      </li>
  <% end %>
</ul>